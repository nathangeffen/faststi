# Eventually faststi.py must be installed as a Python package.
# But we'll do that once it's mature.


meson.add_install_script([
    'check_faststi.sh', 'faststi.py',
    'processResultsFile.py',  'valgrind_faststi.sh',
    'processResultsFile.R'])
